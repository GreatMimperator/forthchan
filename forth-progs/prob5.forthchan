:prob \ ( from, to )
    \ 1 pick \ to
	-1
	\ swap \ start with to
	232792560
	doi	\ o_i, stack is ( o_i, from, to)
		0 3 pick 3 pick		\ adding flag before and dupl from to
		doi				\ stack is ( i, flag, o_i, from, to )
			2 pick swap mod		    \ stack is ( o_i % i, flag, o_i )
			0 <> if
				drop -1 swap	 	\ flag is false - not an answer
				leave
			then
		loop
		swap				\ stack is ( flag, o_i )
		if
			leave
		then
		drop  \ drop o_i
	loop
;

20 1
prob
